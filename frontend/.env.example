# Copy to .env and set your API base URL (no trailing slash).
# Use your backend origin; /api is added automatically if missing.
# Example: http://localhost:5000  or  http://localhost:5000/api
VITE_API_URL=http://localhost:5000
